import { ShowStopsPipe } from './show-stops.pipe';

describe('ShowStopsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowStopsPipe();
    expect(pipe).toBeTruthy();
  });
});
